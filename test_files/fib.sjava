/* Recursion stress test! */

int fib(int x) {
    if ((x == 0) || (x == 1))
       return 1;
    return fib(x-1) + fib(x-2); 
}

void main() {

   int i;
  
   for (i=0; i<30;i++) {
      Print(fib(i));
      Println();
   }
}
